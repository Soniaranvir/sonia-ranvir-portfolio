module.exports = {
  darkMode: 'class',  // Enable class-based dark mode
  content: ['./*.html'],  // Adjust this path if your HTML is in a subfolder
  theme: {
    extend: {},
  },
  plugins: [],
};
